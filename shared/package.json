{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "rm -rf generated/prisma && npx prisma generate --schema=../prisma/schema.prisma",
    "build": "tsc -b",
    "clean": "rm -rf dist generated/prisma"
  },  
  "dependencies": {
    "typescript": "^5",
    "@types/node": "^24.3.0"
  }
}