{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate --schema=../prisma/schema.prisma",
    "build": "tsc -b",
    "clean": "rm -rf dist generated/prisma"
  }
}